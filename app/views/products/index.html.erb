<h1>From roving to lace</h1>
<h3>A place to share your stash, projects and more!</h3>

<%= @products.each do |product| %>
  <div class="booyah-box col-10 offset-1">
    <%= product.name %><br />
    <%= product.description %>
  </div>
  <hr />
<% end %>